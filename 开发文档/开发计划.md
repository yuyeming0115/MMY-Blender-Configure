# MMY Blender Configure 开发计划

## 项目概述

**插件名称**：MMY Blender Configure  
**作者**：会叫喵的鱼  
**目标版本**：Blender 4.5+  
**插件分类**：MMY-Tools  

### 核心功能
一键备份、导入、导出 Blender 配置（快捷键、软件设置、插件），并监控插件加载耗时和报错信息。

### 使用场景
- 多台电脑间同步 Blender 配置
- 升级 Blender 版本前备份配置
- 团队统一配置环境
- 排查插件加载性能问题

---

## 技术架构

### 文件结构（多文件包）

```
MMY-Blender-Configure/
├── __init__.py              # 插件入口，bl_info，注册/注销
├── operators.py             # 备份、导入、导出 Operator
├── ui.py                    # Header 按钮 + Popover 面板
├── addon_timer.py           # 插件耗时采集模块
├── preferences.py           # 插件偏好设置（备份路径等）
└── utils.py                 # 路径工具、zip 打包/解包
```

**为什么选择多文件**：
- 插件耗时监控需在启动早期注入，逻辑独立
- 备份/导入/导出涉及文件 I/O，代码量大
- 便于后续扩展配置类型

### 核心模块职责

| 模块 | 关键类/函数 | 职责 |
|------|------------|------|
| `__init__.py` | `register()` / `unregister()` | 统一注册，启动耗时采集 |
| `addon_timer.py` | `AddonTimerManager` | 记录插件加载耗时和错误 |
| `operators.py` | `MMY_OT_BackupConfig` | 备份配置到 zip |
| | `MMY_OT_ImportConfig` | 从 zip 还原配置 |
| | `MMY_OT_ExportConfig` | 导出配置到指定路径 |
| `ui.py` | `MMY_PT_ConfigPopover` | Popover 面板 UI |
| | `draw_header_button()` | 顶部菜单栏按钮 |
| `preferences.py` | `MMYConfigPreferences` | 存储勾选状态、默认路径 |
| `utils.py` | `pack_to_zip()` / `unpack_from_zip()` | zip 打包/解包 |

---

## 功能设计详解

### 1. UI 布局

#### 顶部菜单栏按钮
- **位置**：最左侧（使用 `TOPBAR_HT_upper_bar.prepend()`）
- **图标**：🧾 文档图标（Blender 内置 `WORDWRAP_ON` 或自定义 PNG）
- **交互**：点击弹出 Popover 面板

#### Popover 面板内容
```
┌─────────────────────────────┐
│ MMY 配置管理                 │
├─────────────────────────────┤
│ 选择配置类型                 │
│ ☑ 用户快捷键                │
│ ☑ 软件设置                  │
│ ☑ 插件                      │
├─────────────────────────────┤
│ [备份] [导入] [导出]         │
├─────────────────────────────┤
│ 插件加载耗时 ⏱              │
│ addon_a    12.3ms           │
│ addon_b    45.6ms  ⚠ ERROR  │
└─────────────────────────────┘
```

### 2. 配置备份/导入/导出

#### 备份格式：`.zip` 压缩包

```
mmy_config_20260511_143000.zip
├── manifest.json            # 元数据（版本、时间戳、包含项）
├── userpref.blend           # 软件设置 + 快捷键 + 插件状态
├── config/                  # 用户配置（bookmarks、recent-files 等）
── addons/                  # 仅当勾选"插件"时包含
│   ├── addon_a/
│   └── addon_b.py
── scripts/presets/         # 用户预设
├── scripts/startup/         # 启动脚本
├── datafiles/               # 数据文件
├── studiolights/            # 工作室灯光预设
└── extensions/              # 扩展包
```

**manifest.json 示例**：
```json
{
  "blender_version": "4.5.0",
  "created_at": "2026-05-11T14:30:00",
  "includes": ["keymap", "prefs", "addons", "config", "presets", "startup", "datafiles"]
}
```

#### 备份流程
1. 调用 `bpy.ops.wm.save_userpref()` 确保最新状态已写入磁盘
2. 从 `bpy.utils.user_resource('CONFIG')` 等 API 获取各目录路径
3. 根据勾选项收集文件：
   - keymap/prefs → `config/userpref.blend`（Blender 不分离存储）
   - addons → `scripts/addons/` 下用户安装的插件
   - config → 用户配置目录（bookmarks、recent-files 等）
   - presets → `scripts/presets/`
   - startup → `scripts/startup/`
   - datafiles → `datafiles/` + `studiolights/` + `extensions/`
4. 写入 `manifest.json`，打包为 zip
5. 保存到默认路径或用户指定路径

#### 导入流程
1. 文件选择器过滤 `.zip`
2. 解析 `manifest.json`，校验 Blender 版本兼容性
3. 覆盖前自动对现有配置做临时备份（`.mmy_temp_backup_<timestamp>.zip`）
4. 按勾选项选择性还原文件
5. 弹出提示：需重启 Blender 生效

#### 导出流程
复用备份逻辑，仅目标路径由用户指定。

### 3. 插件耗时监控

#### 方案：Monkey-patch + 兜底

**主方案**：在 `register()` 中包装 `bpy.utils._addon_utils.enable`

```python
import time, traceback

_original_enable = bpy.utils._addon_utils.enable

def _timed_enable(module_name, *args, **kwargs):
    t0 = time.perf_counter()
    error = None
    try:
        result = _original_enable(module_name, *args, **kwargs)
    except Exception as e:
        error = traceback.format_exc()
        result = None
    elapsed = time.perf_counter() - t0
    addon_timer.manager.record(module_name, elapsed, error)
    return result

bpy.utils._addon_utils.enable = _timed_enable
```

**兜底方案**：`bpy.app.timers` 启动后扫描 `sys.modules`，检测加载失败的插件。

**为什么不用启动日志**：需要用户手动配置 `--debug` 参数，普通用户不友好。

---

## 开发里程碑

### M1 — 骨架与 UI（1-2天）
**目标**：插件可安装，UI 可交互

**任务**：
- [ ] 创建 `__init__.py`，定义 `bl_info`
- [ ] 实现 `ui.py`：Header 按钮 + Popover 面板
- [ ] 实现 `preferences.py`：勾选状态持久化
- [ ] 验证：点击按钮弹出面板，勾选项状态保存

**关键文件**：
- `__init__.py`
- `ui.py`
- `preferences.py`

---

### M2 — 备份功能（2-3天）
**目标**：可将完整配置打包为 zip

**任务**：
- [x] 实现 `utils.py`：路径工具、zip 打包，支持 config/presets/startup/datafiles/studiolights/extensions
- [x] 实现 `operators.py` 中的 `MMY_OT_BackupConfig`
- [x] 生成 `manifest.json`，记录包含的目录列表
- [x] 导入前自动临时备份
- [ ] 验证：备份后 zip 包含所有勾选目录

**关键文件**：
- `utils.py`
- `operators.py`

---

### M3 — 导入/导出功能（2天）
**目标**：可从 zip 还原完整配置

**任务**：
- [x] 实现 `MMY_OT_ImportConfig`：文件选择器 + 解包
- [x] 实现 `MMY_OT_ExportConfig`：复用备份逻辑
- [x] 版本兼容性校验（主版本号不同则警告）
- [x] 覆盖前自动临时备份
- [x] 支持还原 config/presets/startup/datafiles/studiolights/extensions
- [ ] 验证：导入后配置生效（需重启 Blender）

**关键文件**：
- `operators.py`

---

### M4 — 插件耗时监控（2天）
**目标**：面板显示插件加载耗时和错误

**任务**：
- [x] 实现 `addon_timer.py`：`AddonTimerManager` 单例
- [x] Monkey-patch `bpy.utils._addon_utils.enable`
- [x] 兜底方案：`bpy.app.timers` 扫描早期加载的 addon
- [x] UI 中显示耗时列表，错误行高亮
- [ ] 验证：启动 Blender 后面板显示各插件耗时

**关键文件**：
- `addon_timer.py`
- `ui.py`（更新面板布局）

---

### M5 — 打磨与测试（1-2天）
**目标**：生产可用

**任务**：
- [x] 错误处理：zip 损坏检测、版本兼容性警告、文件不存在检查
- [ ] 自定义图标（可选）：加载 PNG 替换内置图标
- [ ] 在 Blender 4.5 实际环境测试全流程
- [x] 编写 README.md 使用说明

---

## 关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 文件结构 | 多文件包 | 逻辑分离，便于维护和扩展 |
| 备份格式 | zip + manifest.json | 跨平台，可选择性还原，含元数据 |
| keymap/prefs 分离 | 不分离，统一备份 userpref.blend | Blender 本身不分离存储，强行分离需解析 blend 文件格式（复杂度过高） |
| 勾选状态存储 | AddonPreferences | Popover 关闭不丢失，随 userpref 持久化 |
| 耗时采集 | Monkey-patch + 兜底 | 精度最高，同时保证稳定性 |
| Header 按钮位置 | `prepend()` 到 TOPBAR_HT_upper_bar | 确保在最左侧 |
| 备份范围 | 支持 config/presets/startup/datafiles/studiolights/extensions | 完整还原用户工作环境，非核心目录由用户按需勾选 |
| 导入安全 | 导入前自动临时备份 | 防止误操作丢失原有配置 |
| addon_utils 路径 | `bpy.utils._addon_utils` | Blender 4.5+ 的标准路径 |

---

## 验证方案

### 端到端测试流程
1. **安装插件**：复制到 `scripts/addons/`，在偏好设置中启用
2. **备份配置**：
   - 修改快捷键、软件设置、安装测试插件
   - 点击"备份"，检查生成的 zip 包
   - 解压验证 `manifest.json` 和 `userpref.blend` 存在
3. **导入配置**：
   - 修改当前配置（改变快捷键）
   - 点击"导入"，选择之前的备份 zip
   - 重启 Blender，验证配置已还原
4. **插件耗时**：
   - 安装多个插件（含一个故意写错的插件）
   - 重启 Blender，打开面板
   - 验证耗时列表显示正常，错误插件标红

### 边界情况测试
- 备份路径不存在 → 自动创建目录
- 导入损坏的 zip → 提示错误，不覆盖现有配置
- 导入不同 Blender 版本的备份 → 警告但允许继续
- 磁盘空间不足 → 提示错误，清理临时文件

---

## 当前进度

- [x] 项目初始化
- [x] 技术调研
- [x] 架构设计
- [x] M1 实现
- [x] M2 实现
- [x] M3 实现
- [x] M4 实现
- [ ] M5 实现（剩余：自定义图标、Blender 实际环境测试）

---

## 变更记录

### 2026-05-21
- **新增**：`pack.py` 自动打包脚本，支持版本号读取、排除缓存文件、输出到 releases/
- **更新**：`.gitignore` 添加 `releases/` 排除规则
- **更新**：插件文件夹重命名为 `mmy_pack_config`
- **更新**：CLAUDE.md 文档引用路径同步更新
- **新增**：`Blender配置导出风险分析.html`，详细分析各配置项的必要性和版本兼容风险
- 新增 `架构设计.md`，规划 MMY Blender Toolkit 模块化重构方案
- 核心设计：模块基类 `MMYModuleBase` + 注册中心 `ModuleRegistry` + 自动发现机制
- UI 整合：单一主面板，各模块按分类展示专属区域
- 迁移计划：框架重构 → 功能迁移 → 新模块开发 → 测试文档
- 创建开发计划文档
- 确定多文件包结构
- 确定 zip + manifest.json 备份格式
- 确定 Monkey-patch 耗时采集方案
- **优化**：扩展备份范围，新增 config/presets/startup/datafiles/studiolights/extensions 支持
- **优化**：导入前自动临时备份，防止误操作
- **修复**：`addon_timer.py` 模块路径改为 `bpy.utils._addon_utils`，兼容 Blender 4.5+
- **优化**：`utils.py` 新增 `_pack_dir` / `_extract_dir_from_zip` 辅助函数，简化目录递归逻辑
- **新增**：M3 版本兼容性校验，主版本号不同时警告用户
- **新增**：M4 兜底方案，`bpy.app.timers` 延迟扫描早期加载的 addon
- **新增**：M5 错误处理增强（zip 损坏检测、文件不存在检查、manifest 校验）
- **新增**：README.md 使用说明文档

### 2026-05-22
- **UI 重构**：`ui.py` 全面优化面板布局，提升易用性
  - 新增环境信息区：显示 Blender 版本号、Portable 模式状态、配置路径
  - 新增备份路径设置区：可编辑默认保存位置
  - 新增使用说明区：简要解释备份/导入/导出功能
  - 新增路径依赖实时检测：勾选"用户配置"时显示警告
  - 新增帮助按钮：弹出完整使用说明（含 Portable 模式迁移指南）
- **偏好设置优化**：`preferences.py` 各配置项添加 `description` tooltip
- **新增 Operator**：`MMY_OT_SelectBackupPath` 选择备份路径、`MMY_OT_ShowHelp` 显示帮助

### 2026-07-02
- **优化**：插件加载耗时面板支持展开/收起超过 30 条后的剩余计时记录
- **新增**：默认隐藏 Blender 内置/官方插件，减少对自研和第三方插件耗时排查的干扰
- **新增**：额外隐藏模块名前缀输入框，可按需屏蔽 `bl_ext.user_default.` 等扩展仓库
- **新增**：`插件耗时监控筛选与展开方案.md` 记录本次筛选与展开交互改动
- **新增**：打包路径记忆保存到 Blender `scripts/presets/mmy_blender_configure/path_memory.json`
- **优化**：打包弹窗会自动恢复并保存上次 Portable 路径和输出目录，插件更新后不再依赖插件目录内配置
- **发布准备**：版本号升级为 `1.1.0`
